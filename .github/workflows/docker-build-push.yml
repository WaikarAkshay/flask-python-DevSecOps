name: build and push docker image
on:
  workflow_call:
    
      
  
jobs:
  build-and-push:
    
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: login docker
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: docker build and push
        uses: docker/build-push-action@v6
        with:
         context: .
         push: true
         tags: |
            ${{ secrets.DOCKERHUB_USER }}/flask-app-image:${{ github.ref_name }}
            ${{ secrets.DOCKERHUB_USER }}/flask-app-image:latest
            ${{ secrets.DOCKERHUB_USER }}/flask-app-image:${{ github.sha}}